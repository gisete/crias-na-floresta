# Payload CMS Secret Key
# Generate a random secret for production: openssl rand -base64 32
PAYLOAD_SECRET=your-secret-here

# Supabase PostgreSQL Database Connection
# Get this from your Supabase project settings under Database > Connection String (URI)
# Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
DATABASE_URI=postgresql://postgres:password@localhost:5432/crias-na-floresta

# Next.js Server URL
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Note: The project is now configured for PostgreSQL with Supabase.
# To get your Supabase connection string:
# 1. Go to your Supabase project dashboard
# 2. Navigate to Settings > Database
# 3. Find "Connection string" and copy the URI format
# 4. Replace [password] with your actual database password
# 5. Paste the full connection string as DATABASE_URI above
